@movies.map do |movie|
  {
    id: movie.id,
    title: movie.title,
    plot: movie.plot,
    director: movie.director,
    english: movie.english,
    created_at: movie.created_at,
    updated_at: movie.updated_at
  }
end
